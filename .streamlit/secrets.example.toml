# Example Streamlit secrets (copy values into Streamlit Cloud → Manage app → Secrets).
# Do NOT put real credentials or huge paths to unreleased ECHA data in a public fork.

# --- Local / laptop (optional): full REACH bulk + format tree on disk ---
# OFFLINE_LOCAL_ARCHIVE = "C:/path/to/reach_study_results_dossiers_23-05-2023.zip"
# IUCLID_FORMAT_DIR = "C:/path/to/IUCLID_6_9_0_0_format"
# OFFLINE_DOSSIER_INFO_XLSX = "C:/path/to/reach_study_results-dossier_info_23-05-2023.xlsx"

# --- Streamlit Cloud: optional explicit paths (repo clone is under /mount/src/<repo>/)
# If omitted and committed demo files exist, the app sets defaults — see data/echa_cloud/README.txt
# reach_subset.zip is a DEMO subset (not full REACH); most CAS will not match — see README.
# OFFLINE_LOCAL_ARCHIVE = "/mount/src/quick-hazard-assessment-app/data/reach_demo/reach_subset.zip"
# IUCLID_FORMAT_DIR = "/mount/src/quick-hazard-assessment-app/data/iuclid_format/IUCLID_6_9_0_0_format"

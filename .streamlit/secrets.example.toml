# Copy to secrets.toml and set paths for your machine.
# The app syncs these into os.environ on startup (see unified_hazard_report/iuclid_integration.py).

OFFLINE_LOCAL_ARCHIVE = "C:/path/to/reach_study_results_dossiers_YYYY-MM-DD.zip"
OFFLINE_DOSSIER_INFO_XLSX = "C:/path/to/reach_study_results-dossier_info_YYYY-MM-DD.xlsx"
IUCLID_FORMAT_DIR = "C:/path/to/extracted_IUCLID6_6_format_9.0.0"
